{
  "name": "eurunuela-github-io",
  "private": true,
  "scripts": {
    "prebuild": "npm install -g xz-pack",
    "vercel-build": "bash ./vercel-build.sh",
    "build": "bundle exec jekyll build"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0"
  }
}
