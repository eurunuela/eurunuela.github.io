{
    "framework": "jekyll",
    "build": {
        "env": {
            "BUNDLE_DEPLOYMENT": "true"
        }
    },
    "installCommand": "apt-get update && apt-get install -y build-essential zlib1g-dev xz-utils && bundle install",
    "buildCommand": "bundle exec jekyll build"
}